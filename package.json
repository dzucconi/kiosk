{
  "name": "kiosk",
  "license": "MIT",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html && yarn build-script",
    "build-script": "parcel build src/script.ts --no-content-hash"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "queryparams": "^1.0.52"
  }
}
